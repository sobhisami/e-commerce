import { StyledAlignFlex } from "@/style/common";
import { styled } from "styled-components";

export const StyledPath = styled(StyledAlignFlex)`
  @media (max-width: 426px) {
    display: none;
  }
`;