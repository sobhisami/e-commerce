import { styled } from "styled-components";

export const StyledFooterListCard = styled.div`
  ul {
    list-style: none;
  }

  ul li {
    color: var(--gray-500-color);
    font-size: 16px;
    font-style: normal;
    font-weight: 400;
    line-height: 24px;
    letter-spacing: -0.2px;
    padding: 5px 0;
    cursor: pointer;
  }
`;