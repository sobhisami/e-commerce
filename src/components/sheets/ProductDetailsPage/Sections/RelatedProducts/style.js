import { StyledSection } from "@/style/common";
import { styled } from "styled-components";

export const StyledRelatedProducts = styled(StyledSection)`
  margin-bottom: 20px;
  h3 {
    margin-bottom: 15px;
  }
`;
